## 🔹 How to Play

- Press **CLICK** to inflate the balloon.
- Reach the click goal before the timer runs out to advance to the next level.
- If you fail the level, you can press **Repeat level** or **Start over**.
- High scores are saved in the browser.

---

## 🔹 Customization

- **main.js** – you can change:
  - `LEVEL_TIME` – level duration (default 30 seconds)
  - `START_GOAL` – initial click goal
  - `GOAL_MULT` – goal multiplier per level

- **style.css** – you can change colors, button sizes, fonts, background, etc.

---

## 🔹 Sounds

- Uses the following mp3 files:
  - `click.mp3` – click sound
  - `fail.mp3` – fail sound
  - `record.mp3` – record achievement sound

> All mp3 files must be in the same folder as `index.html`.

---

## 🔹 Support

- For questions or suggestions: your@email.com
